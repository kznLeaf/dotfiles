lazyvim plugin path:`~/.local/share/nvim/lazy` and cache path: `~/.local/state/nvim/lazy`

Runtime fils should be located in:
- `lua/`
- `plugin`
- `ftplugin`

commands:

Explore installed plugins
- `vim.pack.update(nil, { offline = true })`
- Navigate between plugins with `[[` and `]]`. List them with `gO` 
